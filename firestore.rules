rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /user_data/{userId} {
      allow read, write: if true; // In production this should be auth != null and request.auth.uid == userId, but Clerk is handling auth here.
    }
  }
}
